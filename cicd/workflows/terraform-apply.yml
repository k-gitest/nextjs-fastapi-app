name: Terraform Apply

on:
  workflow_dispatch:
    inputs:
      environment:
        description: Environment to apply
        required: true
        type: choice
        options:
          - staging
          - production
      auto-approve:
        description: Auto approve (skip confirmation)
        required: false
        type: boolean
        default: false
      trigger-deployment:
        description: Trigger application deployment after apply
        required: false
        type: boolean
        default: true
      deployment-strategy:
        description: Deployment strategy
        required: false
        type: choice
        options:
          - auto       # 環境に応じて自動選択
          - parallel   # 並列実行（高速）
          - sequential # 順次実行（安全）
        default: auto

# 並列実行を防ぐ
concurrency:
  group: terraform-apply-${{ inputs.environment }}
  cancel-in-progress: false

jobs:
  apply:
    name: Apply (${{ inputs.environment }})
    runs-on: ubuntu-latest
    outputs:
      apply-success: ${{ steps.apply.outcome == 'success' }}
      deployment-strategy: ${{ steps.strategy.outputs.strategy }}
      neon-host: ${{ steps.tf_outputs.outputs.neon_host }}
      api-url: ${{ steps.env_urls.outputs.api_url }}
      web-url: ${{ steps.env_urls.outputs.web_url }}

    environment:
      name: ${{ inputs.environment }}
      # TODO: Terraform Cloudのプロジェクト名に合わせて変更する
      url: https://app.terraform.io/app/<YOUR_TF_ORG>/workspaces/<YOUR_PROJECT>-${{ inputs.environment }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Determine Deployment Strategy
        id: strategy
        run: |
          STRATEGY="${{ inputs.deployment-strategy }}"

          # auto の場合は環境に応じて決定
          if [ "$STRATEGY" == "auto" ]; then
            if [ "${{ inputs.environment }}" == "production" ]; then
              STRATEGY="sequential"
              echo "🔴 Production: Using sequential deployment (safe)"
            else
              STRATEGY="parallel"
              echo "🔵 Staging: Using parallel deployment (fast)"
            fi
          fi

          echo "strategy=$STRATEGY" >> $GITHUB_OUTPUT

          echo "## 🚀 Deployment Strategy" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Selected:** \`$STRATEGY\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          if [ "$STRATEGY" == "parallel" ]; then
            echo "### ⚡ Parallel Deployment" >> $GITHUB_STEP_SUMMARY
            echo "- Web / API / Worker deploy simultaneously" >> $GITHUB_STEP_SUMMARY
            echo "- ✅ Faster (saves 2-3 minutes)" >> $GITHUB_STEP_SUMMARY
            echo "- ⚠️ Web may be ready before API" >> $GITHUB_STEP_SUMMARY
          else
            echo "### 🛡️ Sequential Deployment" >> $GITHUB_STEP_SUMMARY
            echo "- API deploys first, then Web and Worker" >> $GITHUB_STEP_SUMMARY
            echo "- ✅ Zero user-facing downtime" >> $GITHUB_STEP_SUMMARY
            echo "- ⏱️ Takes longer (waits for API health check)" >> $GITHUB_STEP_SUMMARY
          fi

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ~1.14.0
          cli_config_credentials_token: ${{ secrets.TF_API_TOKEN }}

      - name: Terraform Init
        run: terraform init
        working-directory: terraform/envs/${{ inputs.environment }}

      - name: Terraform Plan
        id: plan
        run: |
          terraform plan -no-color -out=tfplan
          terraform show -no-color tfplan > plan.txt
        working-directory: terraform/envs/${{ inputs.environment }}

      - name: Show Plan Summary
        run: |
          echo "## Terraform Plan (${{ inputs.environment }})" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Changes to be applied:" >> $GITHUB_STEP_SUMMARY
          echo '```terraform' >> $GITHUB_STEP_SUMMARY
          head -n 100 plan.txt >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
        working-directory: terraform/envs/${{ inputs.environment }}

      - name: Terraform Apply
        id: apply
        run: terraform apply -auto-approve tfplan
        working-directory: terraform/envs/${{ inputs.environment }}

      - name: Get Terraform Outputs
        id: tf_outputs
        if: steps.apply.outcome == 'success'
        run: |
          echo "neon_host=$(terraform output -raw neon_host 2>/dev/null || echo '')" >> $GITHUB_OUTPUT
          echo "neon_database=$(terraform output -raw neon_database 2>/dev/null || echo '')" >> $GITHUB_OUTPUT
          echo "b2_bucket=$(terraform output -raw b2_bucket_name 2>/dev/null || echo '')" >> $GITHUB_OUTPUT
        working-directory: terraform/envs/${{ inputs.environment }}
        continue-on-error: true

      - name: Get Environment URLs
        id: env_urls
        if: steps.apply.outcome == 'success'
        run: |
          echo "web_url=${{ vars.WEB_URL }}" >> $GITHUB_OUTPUT
          echo "api_url=${{ vars.FASTAPI_PUBLIC_URL }}" >> $GITHUB_OUTPUT

      - name: Create Deployment Summary
        if: steps.apply.outcome == 'success'
        run: |
          echo "## 🚀 Terraform Apply Completed" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Environment:** \`${{ inputs.environment }}\`" >> $GITHUB_STEP_SUMMARY
          echo "**Status:** ✅ Success" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          if [ "${{ inputs.trigger-deployment }}" == "true" ]; then
            echo "### 🔄 Next: Application Deployment" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "Deployment strategy: **${{ steps.strategy.outputs.strategy }}**" >> $GITHUB_STEP_SUMMARY
          fi

      - name: Notify on Failure
        if: failure()
        run: |
          echo "## ❌ Terraform Apply Failed" >> $GITHUB_STEP_SUMMARY

  # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  # 並列デプロイ（Staging向け）
  # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  trigger-deployment-parallel:
    name: Trigger ${{ matrix.service }} Deployment
    runs-on: ubuntu-latest
    needs: apply
    if: |
      needs.apply.outputs.apply-success == 'true' &&
      inputs.trigger-deployment &&
      needs.apply.outputs.deployment-strategy == 'parallel'

    strategy:
      matrix:
        include:
          - service: Web
            event: deploy-web
          - service: API
            event: deploy-api
          - service: Worker
            event: deploy-worker

    steps:
      - name: Trigger Dispatch
        uses: peter-evans/repository-dispatch@v3
        with:
          token: ${{ secrets.GH_PAT }}
          event-type: ${{ matrix.event }}
          client-payload: |
            {
              "environment": "${{ inputs.environment }}",
              "triggered_by": "terraform-apply",
              "run_id": "${{ github.run_id }}",
              "deployment_strategy": "parallel",
              "api_url": "${{ needs.apply.outputs.api-url }}",
              "web_url": "${{ needs.apply.outputs.web-url }}",
              "neon_host": "${{ needs.apply.outputs.neon-host }}"
            }

      - name: Create Summary
        run: |
          echo "## ⚡ ${{ matrix.service }} Deployment Triggered" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Strategy:** Parallel (simultaneous with other services)" >> $GITHUB_STEP_SUMMARY
          echo "**Event:** \`${{ matrix.event }}\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "[View workflow](https://github.com/${{ github.repository }}/actions)" >> $GITHUB_STEP_SUMMARY

  # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  # 順次デプロイ（Production向け）
  # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  trigger-api-sequential:
    name: Trigger API Deployment (Sequential)
    runs-on: ubuntu-latest
    needs: apply
    if: |
      needs.apply.outputs.apply-success == 'true' &&
      inputs.trigger-deployment &&
      needs.apply.outputs.deployment-strategy == 'sequential'

    steps:
      - name: Trigger API Deployment
        uses: peter-evans/repository-dispatch@v3
        with:
          token: ${{ secrets.GH_PAT }}
          event-type: deploy-api
          client-payload: |
            {
              "environment": "${{ inputs.environment }}",
              "triggered_by": "terraform-apply",
              "run_id": "${{ github.run_id }}",
              "deployment_strategy": "sequential",
              "api_url": "${{ needs.apply.outputs.api-url }}",
              "neon_host": "${{ needs.apply.outputs.neon-host }}"
            }

      - name: Wait for API Health
        run: |
          echo "## 🛡️ Sequential Deployment: API First" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "⏳ Waiting for API to become healthy before deploying Web and Worker..." >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "This ensures zero user-facing downtime." >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          # API のデプロイ完了を待つ（最大5分）
          MAX_WAIT=300
          ELAPSED=0
          INTERVAL=30

          API_URL="${{ needs.apply.outputs.api-url }}"

          echo "Checking API health: $API_URL/health"

          while [ $ELAPSED -lt $MAX_WAIT ]; do
            if curl -f -s -o /dev/null "$API_URL/health"; then
              echo "" >> $GITHUB_STEP_SUMMARY
              echo "✅ API is healthy! (after ${ELAPSED}s)" >> $GITHUB_STEP_SUMMARY
              echo "" >> $GITHUB_STEP_SUMMARY
              echo "Proceeding to Web and Worker deployment..." >> $GITHUB_STEP_SUMMARY
              exit 0
            fi

            echo "⏳ API not ready yet (${ELAPSED}s elapsed)..."
            sleep $INTERVAL
            ELAPSED=$((ELAPSED + INTERVAL))
          done

          echo "" >> $GITHUB_STEP_SUMMARY
          echo "⚠️ API health check timeout (${MAX_WAIT}s)" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Web and Worker deployment will still proceed." >> $GITHUB_STEP_SUMMARY
          exit 0  # タイムアウトしても続行

  trigger-web-sequential:
    name: Trigger Web Deployment (Sequential)
    runs-on: ubuntu-latest
    needs: [apply, trigger-api-sequential]
    if: |
      needs.apply.outputs.apply-success == 'true' &&
      inputs.trigger-deployment &&
      needs.apply.outputs.deployment-strategy == 'sequential'

    steps:
      - name: Trigger Web Deployment
        uses: peter-evans/repository-dispatch@v3
        with:
          token: ${{ secrets.GH_PAT }}
          event-type: deploy-web
          client-payload: |
            {
              "environment": "${{ inputs.environment }}",
              "triggered_by": "terraform-apply",
              "run_id": "${{ github.run_id }}",
              "deployment_strategy": "sequential",
              "web_url": "${{ needs.apply.outputs.web-url }}",
              "api_url": "${{ needs.apply.outputs.api-url }}"
            }

      - name: Create Summary
        run: |
          echo "## 🚀 Web Deployment Triggered" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Strategy:** Sequential (after API health check)" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "[View workflow](https://github.com/${{ github.repository }}/actions)" >> $GITHUB_STEP_SUMMARY

  trigger-worker-sequential:
    name: Trigger Worker Deployment (Sequential)
    runs-on: ubuntu-latest
    needs: [apply, trigger-api-sequential]
    if: |
      needs.apply.outputs.apply-success == 'true' &&
      inputs.trigger-deployment &&
      needs.apply.outputs.deployment-strategy == 'sequential'

    steps:
      - name: Trigger Worker Deployment
        uses: peter-evans/repository-dispatch@v3
        with:
          token: ${{ secrets.GH_PAT }}
          event-type: deploy-worker
          client-payload: |
            {
              "environment": "${{ inputs.environment }}",
              "triggered_by": "terraform-apply",
              "run_id": "${{ github.run_id }}",
              "deployment_strategy": "sequential",
              "api_url": "${{ needs.apply.outputs.api-url }}",
              "neon_host": "${{ needs.apply.outputs.neon-host }}"
            }

      - name: Create Summary
        run: |
          echo "## 🚀 Worker Deployment Triggered" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Strategy:** Sequential (after API health check)" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "[View workflow](https://github.com/${{ github.repository }}/actions)" >> $GITHUB_STEP_SUMMARY